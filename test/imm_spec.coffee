imm              = require('../src/imm.ts')
chai             = require('chai')
expect           = chai.expect

describe 'imm', ->

	# // it 'shows a deprecation messages', function() {
	# // 	imm([])
	# // 